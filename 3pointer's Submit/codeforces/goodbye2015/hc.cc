#include<bits/stdc++.h>

using namespace std;


int main(){
    int n = 500;
    printf("500 500\n");
    for (int i = 0; i < n; i ++){
        for (int j = 0; j < n; j ++){
            printf(".");
        }
        printf("\n");
    }
    int q = 100000;
    printf("%d\n", q);
    for (int i = 0; i < q; i ++){
        printf("%d %d %d %d\n", 2, 2, 500, 500);
    }
    return 0;
}
